/**
 * @prototype
 * @status FAKE — awaiting real backend endpoint
 * @planned-endpoint POST /api/v1/oncology/analyze
 */

export const KNOWN_PROTOCOLS = new Set([
  'AC-T (mama)',
  'FOLFOX (cólon-reto)',
  'FOLFIRI (cólon-reto)',
  'CHOP (linfoma)',
  'R-CHOP (linfoma)',
  'Carboplatina + Paclitaxel (ovário/pulmão)',
  'TC (mama)',
  'EC (mama)',
  'Cisplatina + Etoposídeo (pulmão)',
]);
